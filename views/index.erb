<% if session[:message] %>
  <p id="message"><%= session.delete :message %></p>
<% end %>

<ul>
  <% @files.each do |file| %>
    <li>
        <a href="/<%= file %>"><%= file %></a> ::
      <a href="/<%= file %>/edit">Edit</a></li>
  <% end %>
</ul>